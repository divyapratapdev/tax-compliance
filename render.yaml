services:
  - type: web
    name: taxpilot-backend
    env: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    envVars:
      - key: PORT
        value: 8000
      - key: MONGO_URL
        placeholder: "Insert your MongoDB connection string (e.g., mongodb+srv://...)"
